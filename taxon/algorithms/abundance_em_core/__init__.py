"""Standalone core for taxon abundance estimation via multinomial mixture EM.

This package has no dependency on the ProteomicsAgent runtime; it can be
imported and used in isolation. The plugin wrapper in
``taxon/algorithms/abundance_em.py`` is the only adapter to the agent.
"""
